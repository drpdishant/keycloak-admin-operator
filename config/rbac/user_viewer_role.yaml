# permissions for end users to view users.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: user-viewer-role
rules:
- apiGroups:
  - admin.keycloak.org
  resources:
  - users
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - admin.keycloak.org
  resources:
  - users/status
  verbs:
  - get
